<h1>StaticPages#home</h1>
<% if logged_in? %>
  <div class="row">
    <aside class="col-md-4">
      <section class="user_info">
        <%= render 'shared/user_info' %>
      </section>
      <section class="followings_info">
       <%# 現在のユーザー[current_user.id]がフォローしている人の一覧[id]を表示 %>
       <%= link_to 'followings', followings_user_path(current_user.id) %> 
       <%# <%= link_to 'followings', followings_user_path(params[:id]) %> 
       <%# <%= link_to "followings", "/users/#{params[:id]}/followings" 別の記述方法① %>
       <%# <%= link_to "followings", "/users/#{@id}/followings" 別の記述方法② %>
       <%# <%= link_to "followings", '/users/:id/followings' 間違った記述方法%>
      </section>
      <section class="followers_info">
       <%# 現在のユーザー[current_user.id]をフォローしている人の一覧[id]を表示 %>
       <%= link_to 'followers', followers_user_path(current_user.id) %> 
       <%# <%= link_to 'followers', followers_user_path(params[:id]) %> 
       <%# <%= link_to "followers", '/users/:id/followers' %>
      </section>
      <section class="favorite_info">
       <%# 現在のユーザー[current_user.id]をフォローしている人の一覧[id]を表示 %>
       <%= link_to 'favorites' %> 
       <%# <%= link_to 'followers', followers_user_path(params[:id]) %> 
       <%# <%= link_to "followers", '/users/:id/followers' %>
      </section>
      <section class="micropost_form">
        <%= render 'shared/micropost_form' %>
      </section>
    </aside>
    <div class="col-md-8">
      <h3>Micropost Feed</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
  <div class="center jumbotron">
    <h1>Welcome to the Microposts</h1>

    <%= link_to "Sign up now!", signup_path, class: "btn btn-lg btn-primary" %>
  </div>
<% end %>
<p>Find me in app/views/static_pages/home.html.erb</p>
