<%= form_for(current_user.favorite_relationships.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :fuser_id, @micropost.id %></div>
  <%= f.submit "Favorite", class: "btn btn-primary" %>
<% end %>
