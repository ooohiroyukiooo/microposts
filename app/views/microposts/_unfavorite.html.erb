<%= form_for(current_user.favorite_relationships.find_by(fuser_id: @micropost.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unfavorite", class: "btn" %>
<% end %>
